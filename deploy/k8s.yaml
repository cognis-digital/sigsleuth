apiVersion: apps/v1
kind: Deployment
metadata: { name: sigsleuth, labels: { app: sigsleuth } }
spec:
  replicas: 1
  selector: { matchLabels: { app: sigsleuth } }
  template:
    metadata: { labels: { app: sigsleuth } }
    spec:
      containers:
        - name: sigsleuth
          image: ghcr.io/cognis-digital/sigsleuth:latest
          ports: [{ containerPort: 8000 }]
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata: { name: sigsleuth }
spec:
  selector: { app: sigsleuth }
  ports: [{ port: 80, targetPort: 8000 }]
